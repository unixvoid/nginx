user nobody nogroup;
worker_processes 4;
daemon off;
error_log stderr debug;

events {
	worker_connections 768;
}

http {
	server {
		listen 7000;
		root /cryo/data;
		location / {
			index index.html;
		}
	}
}
